# Multilingual Text Preprocessor

A configurable NLP preprocessing API for English and Arabic text.

## Quick Start (Docker)

### 1. Build the image
```bash
docker build -t text-preprocessor .
```

### 2. Run the container
```bash
docker run -p 8000:8000 text-preprocessor
```

### 3. Open in browser